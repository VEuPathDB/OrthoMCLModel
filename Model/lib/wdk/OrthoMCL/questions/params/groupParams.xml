<?xml version="1.0" encoding="UTF-8"?>
<wdkModel>

    <!-- parameters used by primary key queries -->

    <paramSet name="groupParams">

        <stringParam name="group_name" prompt="Group or Sequence Name" number="false">
            <help>Group or sequence name from current or previous releases. * can be used as wild card</help>
            <suggest default="pfal|PF3D7_02086*" />
        </stringParam>

        <datasetParam name="group_names" prompt="Group Names"
					  recordClassRef="GroupRecordClasses.GroupRecordClass">
            <help>Group name(s), separated by commas or new lines, from current or previous releases.</help>
            <suggest default="OG6_101327, OG6r3_101799" />
        </datasetParam>

        <stringParam name="core_count_min" prompt="Min # Core Proteins" number="true">
            <help>The minimum number of core proteins in the group</help>
            <suggest default="10" />
        </stringParam>

        <stringParam name="core_count_max" prompt="Max # Core Proteins" number="true">
            <help>The maximum number of core proteins in the group</help>
            <suggest default="20" />
        </stringParam>

        <stringParam name="peripheral_count_min" prompt="Min # Peripheral Proteins" number="true">
            <help>The minimum number of peripheral proteins in the group</help>
            <suggest default="10" />
        </stringParam>

        <stringParam name="peripheral_count_max" prompt="Max # Peripheral Proteins" number="true">
            <help>The maximum number of peripheral proteins in the group</help>
            <suggest default="20" />
        </stringParam>

        <stringParam name="core_taxon_min" prompt="Min # Core Taxa" number="true">
            <help>The minimum number of Core taxa present in a group</help>
            <suggest default="5" />
        </stringParam>

        <stringParam name="core_taxon_max" prompt="Max # Core Taxa" number="true">
            <help>The maximum number of Core taxa present in a group</help>
            <suggest default="10" />
        </stringParam>

        <stringParam name="peripheral_taxon_min" prompt="Min # Peripheral Taxa" number="true">
            <help>The minimum number of Peripheral taxa in a group</help>
            <suggest default="5" />
        </stringParam>

        <stringParam name="peripheral_taxon_max" prompt="Max # Peripheral Taxa" number="true">
            <help>The maximum number of Peripheral taxa in a group</help>
            <suggest default="10" />
        </stringParam>

        <stringParam name="percent_min" prompt="Min Percentage" number="true">
            <help>The minimum percentage.</help>
            <suggest default="50" />
        </stringParam>

        <stringParam name="percent_max" prompt="Max Percentage" number="true">
            <help>The maximum percentage</help>
            <suggest default="80" />
        </stringParam>

        <stringParam name="evalue_min" prompt="Min E-Value exponent" number="true">
            <help>The minimum exponent.</help>
            <suggest default="-100" />
        </stringParam>

        <stringParam name="evalue_max" prompt="Max E-Value exponent" number="true">
            <help>The maximum exponent</help>
            <suggest default="-5" />
        </stringParam>

        <stringParam name="phyletic_expression" prompt="Expression" number="false">
            <help>
			<![CDATA[
<p>Phyletic Pattern Expression is a flexible and powerful way to identify ortholog groups with a certain conservation pattern. This tool identifies groups based on whether proteins from specific taxa are present or absent. Also, the tool finds groups with a certain copy number (e.g., duplications) within specified taxa.</p>
			]]>
			</help>
	    <handler implementation="org.orthomcl.service.core.phyletic.ExpressionParamHandler" />
            <suggest default="EUKA>=5T AND hsap>=10" />
        </stringParam>


    <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
    <!-- EC Number Wildcard Parameter -->  
    <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->

        <stringParam name="ec_wildcard"
                        prompt="EC Number wildcard search" number="false">
            <suggest default="N/A"/>
            <help>Asterisks can be used as wildcard characters in an EC Number (e.g. "1.*"). To find genes with any EC Number, you may use "*" or "any".</help>
        </stringParam>



        <enumParam name="text_fields" 
                         prompt="Fields"
                         multiPick="true">
          <help>
                 Choose which text fields to search
          </help>
          <enumList>

            <enumValue default="true">
              <term>sequences (IDs / descriptions / taxa)</term>
              <internal>Sequences</internal>
            </enumValue>

            <enumValue default="true">
              <term>PFam domains (names and descriptions)</term>
              <internal>PFams</internal>
            </enumValue>

            <enumValue default="true">
              <term>EC numbers (and descriptions)</term>
              <internal>EcNumber</internal>
            </enumValue>

            <enumValue default="true">
              <term>ortholog group ID</term>
              <internal>GroupName</internal>
            </enumValue>

          </enumList>
        </enumParam>

    </paramSet>

</wdkModel>
